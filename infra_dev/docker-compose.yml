services:
  db:
    container_name: db_telegram_bot
    image: postgres:14-alpine
    env_file:
      - ../.env
    ports:
      - 5434:5432
    volumes:
      - db_data_telegram_bot:/var/lib/postgresql/data/
volumes:
  db_data_telegram_bot:

networks:
  default:
    name: Network_coffe_bot
